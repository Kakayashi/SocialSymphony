{% extends 'base.html.twig' %}

{% block title %}Profile Settings{% endblock %}

{% block bread %}→ Profile Settings {% endblock %}


{% block body %}
    {{ include ('settings_profile/_settings_profile.html.twig') }}
    {{ form_start(form) }}
    {{ include ('settings_profile/_profile_form.html.twig') }}
    
    <div>
        <button class="block w-full shadow-sm border-transparent bg-indigo-600 hover:bg-indigo-700 darg:bg-indigo-500 dark:hover:bg-indigo-400 text white rounded-md border p-2 mt-4 mb-2" type="submit">Save settings</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
